{
	// SAFE MODE: MCP servers disabled for now. Uncomment to re-enable.
	// Reason: Not currently using MCP integrations; prevent accidental external calls.
	"servers": {
		// "Azure DevOps": {
		// 	"type": "stdio",
		// 	"command": "npx",
		// 	"args": ["-y", "@azure-devops/mcp", "${input:ado_org}" ]
		// },
		// "Microsoft Learn": {
		// 	"url": "https://learn.microsoft.com/api/mcp",
		// 	"type": "http"
		// }
	},
	"inputs": [
		// { "id": "ado_org", "type": "promptString", "description": "Azure DevOps organization name (e.g. 'contoso')" }
	]
}